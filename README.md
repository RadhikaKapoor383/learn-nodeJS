# Learn Node.js

This is a simple Node.js project using Express to serve a JSON API.

## Installation

Run `npm install` to install dependencies.

## Running the Server

Run `npm start` to start the server on http://localhost:3000

The server serves a JSON object with person data at the root endpoint.

## API

- GET / : Returns person data in JSON format